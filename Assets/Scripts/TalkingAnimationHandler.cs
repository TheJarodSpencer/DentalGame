using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TalkingANimationHandler : MonoBehaviour
{
    // Start is called before the first frame update
    public GlobalVariables GV;
    public Material[] talkingFrames;
    //0 is look left close
    //1 is look left open
    //2 is look right closed
    //3 is look right open
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
